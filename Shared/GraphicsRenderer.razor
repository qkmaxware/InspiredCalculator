@switch (Graphic) {
    case PlotGraphic plot:
        <Plot Data=plot.Data Layout=plot.Layout></Plot>
        break;
    case ImageUrl image:
        <Image Url=@(image.Url)></Image>
        break;
}

@code {
    [Parameter] public IGraphics? Graphic {get; set;}
}